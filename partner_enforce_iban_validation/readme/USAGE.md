Put an IBAN into the partner bank account field. A validation error will be raised if the IBAN validation fails.

When putting IBAN through code, use the "skip_iban_validation" context key to skip the check programatically.
