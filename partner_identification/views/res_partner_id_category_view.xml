<odoo>
    <!-- Partner Identification Number Categories -->
    <record id="view_partner_id_category_form" model="ir.ui.view">
        <field name="name">res.partner.id_category.form</field>
        <field name="model">res.partner.id_category</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form string="Partner Identification Categories">
                <sheet>
                    <widget
                        name="web_ribbon"
                        title="Archived"
                        bg_color="text-bg-danger"
                        invisible="active"
                    />
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only" />
                        <h1>
                            <field name="name" placeholder="Category Name" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code" />
                            <field name="color" widget="color_picker" string="Color" />
                            <field
                                name="validation_code"
                                widget="code"
                                placeholder="# Enter your validation code here
# Use variable 'id_number' for the ID number to validate
# Set variable 'failed' to True if validation fails
failed = not id_number.name.isdigit()"
                            />
                        </group>
                        <group>
                            <div class="alert alert-primary" role="alert">
                                <h4>Python Validation Code:</h4>
                                <pre>
# Use failed = True to specify that the id number is not valid.
# You can use the following variables :
#  - self: browse_record of the current ID Category browse_record
#  - id_number: browse_record of ID number to validate

<b>
                                    <i># Sample 1: ID number only contains digits</i>
                                </b>
if not id_number.name.isdigit():
    failed = True
else:
    failed = False

<b>
                                    <i
                                    ># Sample 2: Length of ID number cannot exceed 10 chars</i>
                                </b>
failed = len(id_number.name) > 10 and True or False

<b>
                                    <i
                                    ># Sample 3: ID number must start with the category code</i>
                                </b>
failed = not id_number.name.startswith(self.code) and True or False
                                </pre>
                            </div>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <record id="view_partner_id_category_search" model="ir.ui.view">
        <field name="name">res.partner.id_category.search</field>
        <field name="model">res.partner.id_category</field>
        <field name="type">search</field>
        <field name="arch" type="xml">
            <search string="Partner ID Categories">
                <field name="name" />
                <field name="code" />

                <separator />

                <filter
                    name="archived"
                    string="Archived"
                    domain="[('active','=',False)]"
                    help="Include archived categories"
                />
            </search>
        </field>
    </record>

    <record id="view_partner_id_category_tree" model="ir.ui.view">
        <field name="name">res.partner.id_category.list</field>
        <field name="model">res.partner.id_category</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list>
                <field name="name" />
                <field name="code" />
                <field name="color" widget="color_picker" />
            </list>
        </field>
    </record>
    <record id="action_partner_id_category" model="ir.actions.act_window">
        <field name="name">Partner ID Categories</field>
        <field name="res_model">res.partner.id_category</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_partner_id_category_search" />
    </record>
    <menuitem
        name="Partner ID Numbers"
        id="menu_partner_id_numbers_main"
        parent="contacts.res_partner_menu_config"
        sequence="10"
    />
    <menuitem
        action="action_partner_id_category"
        id="menu_partner_id_category"
        parent="menu_partner_id_numbers_main"
        sequence="10"
    />
</odoo>
