Odoo's `phone_validation` always reformats contacts' phone and mobile numbers
in international format, prepending the country code (e.g.
`+81 90-1234-5678`). This module makes the format configurable.
