Block partner address or country changes when unpaid invoices or pending reconciliations exist.
The lock scope (country only vs. full address) is configurable per company in accounting settings.
Address changes are tracked in an history log on the partner form.
