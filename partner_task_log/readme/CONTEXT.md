This module uses project tasks without a project as simple contact log entries.
That makes it possible to keep a structured history directly from the contact,
with assignees, tags, status, creation date, notes, and chatter, without creating
projects. Log entries are kept archived by default so they do not appear mixed
with regular project tasks or personal private tasks.
